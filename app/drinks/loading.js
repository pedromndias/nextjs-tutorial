// ? If we create a loading.js file, this will be what will be displayed while page.js is fetching data. With daisyUI we can add a simple spinner using className="loading".

const loading = () => {
  return <span className="loading"></span>
}

export default loading